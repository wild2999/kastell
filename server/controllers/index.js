const locations = require('./locations');
const slides = require('./slides');
const projects = require('./projects');
const abouts = require('./abouts');

module.exports = { locations, slides, projects, abouts };